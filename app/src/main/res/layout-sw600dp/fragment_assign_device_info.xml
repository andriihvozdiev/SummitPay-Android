<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true"
    android:background="@color/colorPrimary"
    android:gravity="center"
    android:orientation="vertical"
    android:focusableInTouchMode="true">

    <TextView
        android:id="@+id/welcome_title_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="22dp"
        android:text="@string/assign_device_info_title"
        android:textColor="@color/colorWhite"
        android:textSize="19sp"
        android:gravity="center"/>

    <LinearLayout
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <androidx.appcompat.widget.AppCompatEditText
            android:id="@+id/device_name_edit_text"
            style="@style/WhiteEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/assign_device_info_hint"
            android:maxLines="1"
            android:ellipsize="end"
            android:imeOptions="actionNext"
            android:inputType="textPersonName"
            android:layout_marginBottom="22dp"/>

        <androidx.appcompat.widget.AppCompatEditText
            android:id="@+id/device_location_edit_text"
            style="@style/WhiteEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/assign_device_location_hint"
            android:drawableEnd="@drawable/ic_location_searching"
            android:maxLines="1"
            android:ellipsize="end"
            android:imeOptions="actionDone"
            android:inputType="textPostalAddress"/>

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/register_device_button"
            style="@style/BlueButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/assign_device_info_button_register"
            android:layout_marginTop="35dp" />

    </LinearLayout>

</LinearLayout>
